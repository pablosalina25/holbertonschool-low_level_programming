#include "main.h"

/** void swap_int - a function that swaps the values of two integers.
 * @a, @b @auxiliar
 */

void swap_int(int *a, int *b)
{
	int auxiliar
	auxiliar = *a;
	*a = *b;
	*b = auxiliar;


}
