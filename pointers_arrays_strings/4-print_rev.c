#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * void print_rev - prints a string, in reverse,
 * @s: The input string to be printed.
 *
 * Description: This function prints the string
 * in reverse
 */
kiiiiiiivoid print_rev(char *s)
{
	int i,mitad,len;
	len = strlen(s)
		mita = len/2;

	for (i=0;i<mitad;i++
}
