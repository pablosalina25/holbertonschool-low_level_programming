#include "main.h"
#include <math.h>
/**
 * mul - multiplies two integers.
 * @a: the first integer
 * @b: the second integer
 *
 * Return: the result of a * b
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
