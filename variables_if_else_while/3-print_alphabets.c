#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letra_mayuscula;

	for (letra = 'A'; letra <= 'Z'; letra++)
		putchar(letra);

	putchar('\n');
	return (0);
}
