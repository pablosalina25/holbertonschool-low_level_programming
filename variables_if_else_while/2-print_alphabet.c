#include <stdio.h>

/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */
	int main(void)
	{
		for (char letra 'a'; letra <= 'z' ; letra++)
		putchar(letra);

	}
	putchar('\n');
	return (0);
	}
