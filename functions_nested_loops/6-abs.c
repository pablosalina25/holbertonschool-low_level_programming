#include "main.h"
#include <stdio.h>
/**
 * int _abs(int) prints  the absolute value of an integer.
 * Return Always Nothing
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
