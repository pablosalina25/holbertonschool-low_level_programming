#include <stdio.h>
/**
 * main - This is the main function of the program.
 * It prints the name of the current source
 * file to the standard output.
 * Return: Always returns 0 to
 * indicate successful execution.
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
